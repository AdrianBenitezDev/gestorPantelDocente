{
  "name": "gestor-plantel-docente-functions",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"Add lint config\"",
    "test:subscription": "node --test tests/subscriptionDomain.test.js tests/firestoreRules.static.test.js",
    "migrate:billing:dry-run": "node scripts/migrateBillingAccessOnboarding.js --dry-run",
    "migrate:billing:apply": "node scripts/migrateBillingAccessOnboarding.js --apply"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5",
    "mercadopago": "^2.12.0"
  }
}
