promt 1
Diff lógico por archivo

functions/src/index.js
Refactoricé getUserTenantId para que:
Lea solo /usuarios/{uid}.
Si no existe perfil, lance HttpsError("failed-precondition", ...) con details.code = "subscription_required".
Si tenantId no existe o access.appEnabled !== true, lance el mismo error funcional.
Eliminé completamente la autoasignación por queries secundarias (tenants.ownerUid, tenants.ownerEmail) y el set automático de tenantId.
frontend/js/app.js
Eliminé el flujo de creación automática de tenant desde login:
resolveTenantIdForUser(...) eliminado.
ensureTenantForUser(...) eliminado.
ensureUserProfileDocument(...) (auto-bootstrap) reemplazado por lectura pura: getUserProfileDocument(uid).
Agregué gating central basado en /usuarios/{uid}:
normalizeBillingStatus(...)
resolveGatingRouteByProfile(...)
redirectToRouteIfNeeded(...)
loadAuthenticatedTenantState(user)
Actualicé onAuthStateChanged para usar ese gating:
Sin auth -> mantiene vista login.
Sin /usuarios/{uid} -> redirige a /registro.html.
appEnabled === true y tenantId presente -> entra a app y carga tenant.
billing.status en pending_confirmation | rejected | cancelled | expired | paused | error -> /estado-suscripcion.html.
billing.status en null | pending_checkout -> /activar-plan.html.
frontend/activar-plan.html
Agregada página mínima (sin checkout) para que el routing nuevo no rompa navegación.
frontend/estado-suscripcion.html
Agregada página mínima (sin integración MP) para estados no activos.
Validación

Ejecuté chequeo de sintaxis:
node --check functions/src/index.js
node --check frontend/js/app.js
Sin errores de sintaxis.
Supuestos aplicados

/usuarios/{uid} pasa a ser fuente principal de truth para gating.
No implementé Mercado Pago ni checkout (solo gating/tenant).
Como no existían activar-plan.html y estado-suscripcion.html, las creé mínimas para evitar rutas rotas.
Usuarios legacy sin access.appEnabled === true quedarán fuera de app hasta migración (coherente con la regla estricta solicitada).

SEGUNDO PROMT:
Implementé el bloque solicitado: modelo base billing/access/onboarding + refactor de registerUser sin tenant inmediato.

Cambios concretos

functions/src/index.js (registerUser)
Ya no crea tenant al registrar.
Crea Auth + perfil base en /usuarios/{uid} con:
tenantId: null
billing.planCode: null
billing.status: null
billing.lastAttemptId: null
billing.mpPreapprovalId: null
access.appEnabled: false
access.reason: "payment_required"
onboarding.accountCreated: true
onboarding.checkoutStarted: false
onboarding.subscriptionActivated: false
onboarding.tenantProvisioned: false
Mantiene verificationLink.
Respuesta ahora devuelve tenantId: null y mensaje de cuenta base.
functions/src/index.js (soporte billingPlans/plan_pro)
Agregué buildPlanProDefaults(...) y ensurePlanProSupport(...).
ensurePlanProSupport() crea/actualiza billingPlans/plan_pro de forma idempotente.
Se invoca desde:
registerUser (garantiza plan base disponible al alta).
mercadoPagoSetupStatus (devuelve plan desde la configuración persistida).
frontend/js/registro.js (compatibilidad UI)
Ajusté el mensaje para no asumir tenant inmediato:
muestra pendiente de suscripcion cuando tenantId viene null.
texto actualizado a “Cuenta base creada...”.
No implementado (por pedido)

No agregué webhook.
No agregué checkout.
No toqué integración activa de Mercado Pago más allá del soporte de plan en Firestore.
Impacto en otras partes del repo por quitar tenant inmediato

Backend: callables que dependen de getUserTenantId(uid) van a bloquear usuario base (sin pago activo), devolviendo subscription_required.
Referencias en functions/src/index.js, index.js, index.js, index.js, index.js, index.js, index.js, index.js, index.js, index.js.
Frontend autenticado (ya refactorizado en bloque previo):
El gating en frontend/js/app.js usa /usuarios/{uid} y redirige usuarios sin tenant/appEnabled a rutas de activación/estado.
Registro:
El usuario registrado ya no queda listo para operar módulos tenant-dependent hasta activación futura.
Chequeos

node --check functions/src/index.js OK.
node --check frontend/js/registro.js OK.

promt 3
Implementación completa hecha para este bloque, sin webhook ni activación de tenant.

Cambios por archivo

functions/src/index.js
Nuevo callable startSubscriptionCheckout:

valida auth
valida planCode === "plan_pro"
valida plan en /billingPlans/plan_pro y que esté activo
crea billingAttempt nuevo
marca intentos previos isCurrent = false
crea preapproval en Mercado Pago
actualiza /usuarios/{uid}:
billing.planCode = "plan_pro"
billing.status = "pending_checkout"
billing.lastAttemptId = attemptId
billing.mpPreapprovalId = mpPreapprovalId
onboarding.checkoutStarted = true
devuelve { ok, attemptId, initPoint }
Helpers agregados:

normalizePlanCode
createMercadoPagoPreapproval
Soporte plan:

Se usa ensurePlanProSupport() para asegurar existencia de /billingPlans/plan_pro antes de checkout.
registerUser sigue sin crear tenant (ya refactorizado en bloque anterior).
frontend/registro.html
Agregada sección visual de plan único:
“Plan Pro - ARS 2000 / mes”
Agregado hidden input:
#reg-plan-code con valor plan_pro
frontend/js/registro.js
Flujo actualizado:
llama registerUser
hace sign-in con email/password (signInWithEmailAndPassword)
llama startSubscriptionCheckout({ planCode })
redirige a initPoint
Si falla startSubscriptionCheckout:
muestra error
deja cuenta base recuperable
informa continuar luego desde /activar-plan.html
Notas de compatibilidad

No se crea tenant en registro ni en checkout (como pediste).
No se implementó webhook ni activación final.
Este flujo depende de que esté configurado el secret MP_ACCESS_TOKEN.
Si checkout falla, el usuario queda creado y recuperable para reintento.
Validé sintaxis:
node --check functions/src/index.js
node --check frontend/js/registro.js

PROMT 4

Implementación lista.

**Cambios principales**
1. **Webhook HTTP + procesamiento desacoplado**
- Agregué `mercadoPagoWebhook` en [index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:1290).
- Flujo:
  - extrae `headers` + `payload`
  - valida firma (`x-signature` + `x-request-id`) con `MP_WEBHOOK_SECRET`
  - persiste evento en `/billingEvents` con `autoId`
  - responde `200` inmediatamente
  - delega procesamiento a helper separado (`processMercadoPagoWebhookEvent`) sin bloquear la respuesta.

2. **Procesamiento de webhook**
- Helper en [index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:509):
  - resuelve `preapprovalId`
  - consulta estado real en Mercado Pago
  - mapea estados con [mapMercadoPagoStatusToBillingStatus](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:241):
    - `authorized -> active`
    - `pending -> pending_confirmation`
    - `paused -> paused`
    - `cancelled -> cancelled`
    - `rejected -> rejected`
    - `expired -> expired`
    - default -> `error`
  - actualiza `billingAttempt` y `/usuarios/{uid}.billing`
  - usa colección auxiliar `/billingPreapprovals/{mpPreapprovalId}` para lookup/soporte.

3. **Activación idempotente y transaccional**
- Agregué `ensureBillingActivation(uid, preapprovalData)` en [index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:394).
- Comportamiento:
  - transacción sobre usuario
  - si ya existe `tenantId` + `access.appEnabled=true`, no duplica tenant
  - si no existe tenant:
    - crea `tenantId`
    - crea `/tenants/{tenantId}`
    - crea estructura mínima:
      - `/tenants/{tenantId}/configuraciones/turnosAndHorarios`
      - `/tenants/{tenantId}/botones/config`
    - actualiza usuario:
      - `tenantId`
      - `billing.status=active`
      - `billing.activatedAt`
      - `access.appEnabled=true`
      - `access.reason=active_subscription`
      - `access.enabledAt`
      - `onboarding.subscriptionActivated=true`
      - `onboarding.tenantProvisioned=true`
      - `onboarding.tenantProvisionedAt`

4. **Checkout callable ya integrado con colección auxiliar**
- `startSubscriptionCheckout` quedó en [index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:1486) y persiste también en `/billingPreapprovals/{mpPreapprovalId}` (lookup rápido).

**Decisiones de idempotencia**
1. `ensureBillingActivation` evita duplicados por transacción + chequeo de estado activo previo.
2. Webhook persiste evento primero y marca `processed/status` para trazabilidad por evento.
3. Si firma inválida: evento queda auditado y se marca `ignored` (no procesa activación).

**Dependencias nuevas / requisitos**
1. No agregué paquetes externos (uso `crypto` nativo de Node).
2. `mercadoPagoWebhook` requiere secrets:
   - `MP_WEBHOOK_SECRET`
   - `MP_ACCESS_TOKEN`
3. Nuevas colecciones usadas:
   - `/billingEvents`
   - `/billingPreapprovals`

**Nota técnica**
- El procesamiento está desacoplado vía helper async luego del `200`. Es separable y claro, pero no es cola durable. Si querés robustez máxima, siguiente paso es mover ese dispatch a Cloud Tasks/PubSub.

Sintaxis validada con `node --check functions/src/index.js`.

PROMT 5
Implementación hecha con routing coherente y sin activar tenant antes de suscripción activa.

**Backend**
1. [functions/src/index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:1775)
- Nuevo callable `getSubscriptionStatus`:
  - devuelve `billingStatus`, `appEnabled`, `reason`, `planCode`, `tenantId`, `nextRoute`
  - `nextRoute` se resuelve con reglas centralizadas (`/registro.html`, `/activar-plan.html`, `/estado-suscripcion.html`, `/horarios.html`)

2. [functions/src/index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:1812)
- Nuevo callable `syncSubscriptionStatus`:
  - usa primero `usuarios/{uid}.billing.mpPreapprovalId`
  - si no existe, busca último intento con `mpPreapprovalId` (`resolveLatestUserPreapprovalId`)
  - si no encuentra ninguno: `failed-precondition` con `code: "subscription_not_found"`
  - consulta estado real en Mercado Pago
  - actualiza estado local
  - activa tenant si corresponde (vía `ensureBillingActivation`)

3. [functions/src/index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:1413)
- Webhook `mercadoPagoWebhook` ya operativo:
  - extrae headers/payload
  - valida firma
  - guarda evento en `/billingEvents` con `autoId`
  - responde `200` temprano
  - delega procesamiento a helper separado (`processMercadoPagoWebhookEvent`)

4. [functions/src/index.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/functions/src/index.js:564)
- `ensureBillingActivation(uid, preapprovalData)`:
  - idempotente + transaccional
  - no duplica tenant si ya existe y acceso activo
  - crea tenant y estructura mínima cuando falta
  - actualiza user con `billing/access/onboarding` + timestamps

**Frontend**
1. [frontend/activar-plan.html](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/activar-plan.html:1)  
2. [frontend/js/activarPlan.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/js/activarPlan.js:1)
- Pantalla de activación:
  - botón `Suscribirme` -> `startSubscriptionCheckout` -> redirect `initPoint`
  - consulta `getSubscriptionStatus`
  - redirección automática según `nextRoute`
  - logout

3. [frontend/estado-suscripcion.html](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/estado-suscripcion.html:1)  
4. [frontend/js/estadoSuscripcion.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/js/estadoSuscripcion.js:1)
- Pantalla de estado:
  - muestra estado actual (`getSubscriptionStatus`)
  - botón `Reintentar pago` -> `startSubscriptionCheckout`
  - botón `Sincronizar estado` -> `syncSubscriptionStatus`
  - redirección automática cuando corresponde (`nextRoute`)
  - logout

5. [frontend/js/app.js](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/js/app.js:1115)
- Integración adicional:
  - `updatePlanBadge(profile)` para mostrar badge **Plan Pro** cuando `access.appEnabled === true`

6. [frontend/horarios.html](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/horarios.html:32)  
7. [frontend/css/styles.css](c:/Users/37adr/OneDrive/Documentos/dev%202026/GestorPlantelDocente/frontend/css/styles.css:190)
- Agregado badge visual `#user-plan-badge` + estilos `.plan-badge`

**Decisiones de idempotencia**
1. Activación de tenant en transacción (no duplica tenant).
2. Webhook registra evento y marca `processed/status`.
3. Sincronización usa lookup estable por `billingPreapprovals` + fallback a attempts.
4. Mapping de estados MP aplicado en helper único (`mapMercadoPagoStatusToBillingStatus`).

**Dependencias nuevas**
1. `crypto` nativo de Node (sin paquetes npm nuevos).
2. Uso de secrets ya existentes: `MP_ACCESS_TOKEN`, `MP_WEBHOOK_SECRET`.
3. Colección auxiliar: `/billingPreapprovals/{mpPreapprovalId}`.

**Validación**
- `node --check functions/src/index.js` OK.
- `node --check frontend/js/app.js` OK.
- En este entorno, `node --check` para los 2 módulos nuevos de frontend falló por `EPERM` del sandbox al resolver ruta, no por sintaxis reportada del archivo.


PROMT 6
Implementé los 3 bloques pedidos: reglas, índices y script de migración con `dry-run/apply`.

**Cambios de código**

1. Reglas Firestore endurecidas en [firestore.rules](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:1)
- `/usuarios/{uid}`:
  - lectura solo del propio usuario.
  - update solo con campos de perfil permitidos (`nombre`, `contacto`, `correoAlt`, `distrito`, `nivel`, `escuela`, `usuario`, `updatedAt`).
  - bloqueo explícito de cambios en `tenantId`, `billing`, `access`, `onboarding` y otros campos sensibles.
- `/billingPlans/{planId}`: solo lectura cliente.
- `/billingAttempts/{attemptId}`: sin escritura cliente (lectura solo del owner por `uid`).
- `/billingEvents/{eventId}`: sin lectura/escritura cliente.
- `/tenants/**` se mantiene habilitado para cliente autenticado por compatibilidad actual del frontend.
- fallback global deny.

Referencias:
- [firestore.rules:25](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:25)
- [firestore.rules:43](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:43)
- [firestore.rules:48](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:48)
- [firestore.rules:53](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:53)
- [firestore.rules:58](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:58)

2. Índices en [firestore.indexes.json](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.indexes.json:1)
- `billingAttempts`: compuesto `uid + createdAt desc`.
- `billingAttempts`: fieldOverrides para `mpPreapprovalId` y `externalReference`.
- `billingEvents`: compuesto `preapprovalId + receivedAt desc`.

Referencias:
- [firestore.indexes.json:4](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.indexes.json:4)
- [firestore.indexes.json:18](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.indexes.json:18)
- [firestore.indexes.json:35](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.indexes.json:35)
- [firestore.indexes.json:49](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.indexes.json:49)

3. Script de migración admin en [functions/scripts/migrateBillingAccessOnboarding.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/scripts/migrateBillingAccessOnboarding.js:1)
- Modo `--dry-run` y `--apply`.
- Completa base `billing/access/onboarding` en usuarios no ambiguos.
- Exporta inconsistencias:
  - `user_with_tenant_without_billing`
  - `billing_active_without_tenant`
  - `tenant_without_owner_uid`
  - `user_with_tenant_without_clear_owner`
  - `tenant_without_minimum_data`
- No autocorrige casos ambiguos: los reporta y los omite en `apply`.
- Exporta JSON a `docs/migraciones/...`.

Referencias:
- [migrateBillingAccessOnboarding.js:15](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/scripts/migrateBillingAccessOnboarding.js:15)
- [migrateBillingAccessOnboarding.js:149](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/scripts/migrateBillingAccessOnboarding.js:149)
- [migrateBillingAccessOnboarding.js:179](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/scripts/migrateBillingAccessOnboarding.js:179)
- [migrateBillingAccessOnboarding.js:299](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/scripts/migrateBillingAccessOnboarding.js:299)
- [migrateBillingAccessOnboarding.js:403](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/scripts/migrateBillingAccessOnboarding.js:403)

4. Scripts npm agregados en [functions/package.json](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/package.json:13)
- `migrate:billing:dry-run`
- `migrate:billing:apply`

---

**Cómo usar**

Desde la raíz del repo:

```bash
npm --prefix functions run migrate:billing:dry-run -- --project horario-escuelas
```

Revisar reporte generado en `docs/migraciones/`.

Si está OK:

```bash
npm --prefix functions run migrate:billing:apply -- --project horario-escuelas
```

Luego desplegar reglas + índices:

```bash
firebase deploy --only firestore:rules,firestore:indexes
```

---

**Advertencias importantes**

- Las nuevas reglas dejan deny por defecto fuera de rutas explícitas; si algún cliente usa colecciones fuera de `tenants/**`, `usuarios`, `billingPlans`, podría romperse y hay que habilitarlo explícitamente.
- El script **no corrige** casos ambiguos (owner no claro, billing activo sin tenant, etc.); solo los exporta para revisión manual.
- `tenant_without_minimum_data` puede listar muchos tenants si faltan campos históricos (`distrito/nivel/escuela/tenantId/createdAt/updatedAt`).

---

**Validación que pude correr**
- `firestore.indexes.json` parsea correcto.
- Validación sintáctica del script por parse JS (`new Function(...)`) correcta.
- No pude ejecutar `node --check <archivo>` directamente en este sandbox por `EPERM` del entorno (ruta OneDrive), pero el archivo está parseado sin error.

promt 7

Implementado. Dejé tests automatizados para lo critico y un plan manual detallado para los 10 casos.

**Archivos creados/modificados**
1. Lógica compartida de suscripción (extraída para testear):
- [subscriptionDomain.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/subscriptionDomain.js:1)
- [index.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:9) ahora importa esa lógica.

2. Tests automatizados:
- [subscriptionDomain.test.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/tests/subscriptionDomain.test.js:11)
  - mapping MP -> billing
  - normalización de estado
  - routing por perfil (`/registro`, `/activar-plan`, `/estado-suscripcion`, `/horarios`)
- [firestoreRules.static.test.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/tests/firestoreRules.static.test.js:14)
  - valida guardas de reglas para `billingAttempts`, `billingEvents`, `billingPlans` y campos sensibles en `/usuarios`.

3. Script npm para correr tests:
- [package.json](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/package.json:13)
  - `test:subscription`

4. Plan de test manual completo (10 casos):
- [plan_tests_suscripcion.txt](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/docs/plan_tests_suscripcion.txt:1)

**Cobertura por casos**
- Automatizados: `1` (parcial por estado/ruta), `2` (parcial), `3`, `8`, `10` (estatico de reglas).
- Manual detallado: `4`, `5`, `6`, `7`, `9` (y validación E2E completa de `1` y `2`).

**Cómo correr**
```bash
npm --prefix functions run test:subscription
```

No pude ejecutar los tests en este sandbox por una restricción de entorno (`EPERM` al resolver rutas de Node), pero sí validé sintaxis de los archivos nuevos/modificados.


ultimo promp
**1) Diff lógico por archivo**

1. [functions/src/index.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:27)  
- Webhook durable con Cloud Tasks:
  - Importé `onTaskDispatched` y `getFunctions` ([index.js:2](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:2), [index.js:6](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:6)).
  - Cola definida: `MP_WEBHOOK_TASK_QUEUE_NAME` ([index.js:27](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:27)).
  - Enqueue helper: `enqueueMercadoPagoWebhookTask(...)` ([index.js:298](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:298)).
  - Worker durable: `processMercadoPagoWebhookTask` con retries (`onTaskDispatched`) ([index.js:1445](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:1445)).
  - `mercadoPagoWebhook` ahora:
    - persiste evento
    - responde `200`
    - encola (`status: enqueued`) ([index.js:1564](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:1564))
    - si falla enqueue: `status: enqueue_failed` + fallback inline documentado ([index.js:1580](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:1580), [index.js:1595](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:1595)).
- `processMercadoPagoWebhookEvent(...)` endurecido para cola:
  - `sourceLabel`
  - `throwOnError`
  - retry-friendly (`processed=false` en error) ([index.js:698](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:698)).
- Provisión mínima de tenant ampliada en `ensureBillingActivation`:
  - `configuraciones/turnosAndHorarios` ([index.js:618](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:618))
  - `configuraciones/pacExtraccion` ([index.js:628](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:628))
  - `configuraciones/encabezadoPac` ([index.js:643](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:643))
  - `botones/config` ([index.js:666](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/functions/src/index.js:666))

2. [firestore.rules](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:12)  
- Nuevo control de pertenencia activa a tenant: `hasActiveTenantAccess(tenantId)` ([firestore.rules:12](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:12)).
- `tenants/**` endurecido por rutas reales del frontend:
  - `configuraciones/turnosAndHorarios` y `tunosAndHorarios` rw
  - `botones/*` rw
  - `docentes/*` rw
  - `cursos/*` read
  - `cursos/*/items/*` rw
  - resto de `tenants/**` deny ([firestore.rules:67](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:67), [firestore.rules:93](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/firestore.rules:93)).

3. [frontend/js/pac.js](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/frontend/js/pac.js:93)  
- Evité errores inesperados pre-activación:
  - estado `hasTenantAccess` ([pac.js:93](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/frontend/js/pac.js:93))
  - `refreshPacTenantAccess()` vía `getSubscriptionStatus` ([pac.js:307](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/frontend/js/pac.js:307))
  - cargas remotas PAC solo si hay tenant activo ([pac.js:535](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/frontend/js/pac.js:535), [pac.js:822](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/frontend/js/pac.js:822))
  - mensaje específico para `subscription_required` ([pac.js:1226](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/frontend/js/pac.js:1226)).

4. [docs/suscripciones-operacion-produccion.md](/c:/Users/37adr/OneDrive/Documentos/dev 2026/GestorPlantelDocente/docs/suscripciones-operacion-produccion.md:1)  
- Checklist operativo completo:
  - secrets
  - migración dry-run/revisión/apply
  - deploy functions/rules/indexes
  - verificación E2E
  - rutas abiertas en `tenants/**`
  - estructura mínima de tenant
  - callables tenant-gated.

---

**2) Qué quedó resuelto**

1. Webhook durable implementado en código con Cloud Tasks + worker dedicado.
2. Reglas de `tenants/**` cerradas a lo estrictamente necesario y por pertenencia activa al tenant.
3. Provisión mínima del tenant reforzada para evitar fallos al primer ingreso.
4. Callables tenant-scoped revisados: frontend PAC ya no dispara errores ruidosos antes de activación.
5. Documento operativo de producción creado con pasos concretos.

---

**3) Qué quedó preparado pero requiere infraestructura**

1. Cloud Tasks en producción requiere:
- API `cloudtasks.googleapis.com` habilitada.
- IAM del runtime para encolar (`roles/cloudtasks.enqueuer`).
- Deploy de functions para materializar queue/worker.

2. Fallback inline ya está implementado y documentado, pero es solo continuidad operativa si la cola no está lista.

---

**4) Riesgos restantes antes de producción**

1. Si Cloud Tasks no está bien configurado, el sistema cae en fallback inline (funciona, pero sin durabilidad plena).
2. Tenants legacy con perfil usuario inconsistente (`usuarios/{uid}.tenantId`/`access.appEnabled`) pueden quedar bloqueados por rules endurecidas hasta corregir datos.
3. No pude ejecutar pruebas integrales en este sandbox por restricciones `EPERM` de Node/rutas; sí validé sintaxis de backend modificado (`functions/src/index.js`).  
4. `frontend/js/pac.js` (módulo ESM browser) no se pudo validar con `node --check` en este entorno por limitaciones del sandbox, pero los cambios son acotados al gating y manejo de error.